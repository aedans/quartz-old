%%
#include <stdio.h>
%%

fn main() {
    val l = if (false()) "Failed" else helloWorld()
    printf(if (true()) l else "Failed")
}

fn helloWorld(): char[] = "Hello, World!"

fn true(): int = 1

fn false(): int = 0

extern_fn printf(char[])
