import std.c.stdio

fn main(): int {
    let l = if _false() then "Failed" else helloWorld();
    (if _true() then l else "Failed").printf();
    return 0;
}

fn helloWorld(): *char {
    return "Hello, World!";
}

fn _true(): int {
    return 1;
}

fn _false(): int {
    return 0;
}
