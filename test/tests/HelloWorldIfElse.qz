%%
#include <stdio.h>
%%

fn main(): int {
    (if (false()) "Failed" else helloWorld()).printf();
    return 0;
}

fn helloWorld(): char[] = "Hello, World!"

fn true(): int = 1

fn false(): int = 0

extern fn printf(char[], ...)
