%%
#include <stdio.h>
%%

fn main()
    (if (false()) "Failed" else helloWorld()).printf()

fn helloWorld(): char[] = "Hello, World!"

fn true(): int = 1

fn false(): int = 0

extern_fn printf(char[])
