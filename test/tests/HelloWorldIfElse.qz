%%
#include <stdio.h>
%%

fn main(): int {
    (if (_false()) "Failed" else helloWorld()).printf();
    return 0;
}

fn helloWorld(): char[] = "Hello, World!"

fn _false(): int = 0

extern fn printf(char[], ...)
