%%
#include <stdio.h>
%%

fn main(): int {
    val added = 1 + 2;
    val subtracted = 2 - 1;
    val multiplied = added * subtracted;
    val divided = subtracted / added;
    printf("7 = %d", added + subtracted + multiplied + divided);
    return 0;
}

extern fn printf(char[], ...)
