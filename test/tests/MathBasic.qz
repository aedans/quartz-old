import std.c.stdio

fn main(): int {
    let added = 1 + 2;
    let subtracted = 2 - 1;
    let multiplied = added * subtracted;
    let divided = subtracted / added;
    printf("7 = %d", added + subtracted + multiplied + divided);
    return 0;
}
