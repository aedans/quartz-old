import std.cstdio

fn main(): int {
    10.countTo();
}

fn countTo(max: int) {
    val i = 0;
    while (i < max)
        printInt(%%i++%%);
    printInt(max);
}

fn printInt(i: int)
    printf("%d ", i);
