import std.c.stdio

fn main(): int {
    10.countTo();
    return 0;
}

fn countTo(max: int) {
    val i = 0;
    while (i < max)
        printInt(%%i++%%);
    printInt(max);
}

fn printInt(i: int)
    printf("%d ", i);
