%
#include <stdio.h>
%

fn main() {
    countTo(10)
}

fn countTo(max: int) {
    val i = 0
    while (lessThan(i, max)) {
       print(%i++%)
    }
    print(max)
}

fn lessThan(i1: int, i2: int): int = %i1 < i2%

fn print(i: int) = %printf("\%d ", i)%
