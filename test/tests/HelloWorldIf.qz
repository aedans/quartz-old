import std.c.stdio

fn main(): int {
    if _true() then helloWorld().printf();
    if _false() then "Failed".printf();
    return 0;
}

fn helloWorld(): *char {
    return "Hello, World!";
}

fn _true(): bool {
    return 1;
}

fn _false(): bool {
    return 0;
}
