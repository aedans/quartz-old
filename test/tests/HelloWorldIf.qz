%
#include <stdio.h>
%

fn main() {
    if (true()) {
        printf(helloWorld())
    }
    if (false()) {
        printf("Failed")
    }
}

fn helloWorld(): char[] = "Hello, World!"

fn true(): int = 1

fn false(): int = 0

extern_fn printf(char[])
