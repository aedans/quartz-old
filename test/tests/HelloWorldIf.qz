%
#include <stdio.h>
%

fn main(): int {
    if (true()) {
        printf(helloWorld())
    }
    if (false()) {
        printf("Failed")
    }
    return 0
}

fn helloWorld(): char[] = "Hello, World!"

fn true(): int = 1

fn false(): int = 0

extern_fn printf(char[])
