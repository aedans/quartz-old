import std.c.stdio

fn main(): int {
    let counter = countTo;
    counter(10, print);
    return 0;
}

fn countTo(i: int, printer: (int) -> void) {
    let counter = countTo;
    if i > 0 then (i - 1).counter(printer);
    printer(i);
}

fn print(i: int) {
    printf("%d ", i);
}
