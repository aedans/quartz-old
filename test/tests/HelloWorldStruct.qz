%%
#include <stdio.h>
%%

struct String {
    val value: char[]
}

fn main(): int {
    "Hello, World!".String().print();
    return 0;
}

fn print(string: String)
    string.value.printf();

extern fn printf(char[], ...)
