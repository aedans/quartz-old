import std.c.stdio

typealias string = char[]

fn main(): int {
    val _greeter = greeter;
    _greeter().invoke<string>().printf();
    return 0;
}

fn <O> invoke(function: () -> O): O {
    return function();
}

fn greeter(): () -> string = helloWorld

fn helloWorld(): string = "Hello, World!"
