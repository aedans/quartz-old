import std.c.stdio

fn main(): int {
    print(identity<const char *>);
    return 0;
}

fn print(func: (const char *) -> const char *) {
    printf(func("Hello, World!"));
}

fn <T> identity(t: T): T = t
