/*
import std.c.stdio

typealias string = const char *
typealias printf = (string, ...) -> int

fn main(): int {
   ({ return printf } as () -> printf)()({ return "Hello, World!" }());
   return 0;
}
*/

fn main(): int {
    return 0;
}
