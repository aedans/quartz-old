import std.c.stdio

fn main(): int {
    val _greeter = greeter;
    _greeter()().printf();
    return 0;
}

fn greeter(): () -> const char* = helloWorld

fn helloWorld(): const char* = "Hello, World!"
