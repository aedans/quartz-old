import std.c.stdio

fn main(): int {
    let _greeter = greeter;
    _greeter()().printf();
    return 0;
}

fn greeter(): () -> *char {
    return helloWorld;
}

fn helloWorld(): *char {
    return "Hello, World!";
}
