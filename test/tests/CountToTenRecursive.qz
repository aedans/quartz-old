%%
#include <stdio.h>
%%

fn main()
    countTo(10)

fn countTo(i: int) {
    if (i > 0)
        countTo(i - 1)
    printInt(i)
}

fn printInt(i: int)
    printf("%d ", i)

extern_fn printf(char[], int)
