%
#include <stdio.h>
%

fn main() {
    countTo(10)
}

fn countTo(i: int) {
    if (greaterThan(i, 0)) {
        countTo(sub(i, 1))
    }
    print(i)
}

fn greaterThan(i1: int, i2: int): int = %i1 > i2%

fn sub(i1: int, i2: int): int = %i1 - i2%

fn print(i: int) = %printf("\%d ", i)%
