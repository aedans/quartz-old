%%
#include <stdio.h>
%%

fn main(): int {
    10.countTo();
    return 0;
}

fn countTo(i: int) {
    if (i > 0)
        (i - 1).countTo();
    print(i);
}

fn print(i: int)
    printf("%d ", i);

extern fn printf(char[], ...)
