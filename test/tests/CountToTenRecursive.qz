%%
#include <stdio.h>
%%

fn main()
    10.countTo()

fn countTo(i: int) {
    if (i > 0)
        (i - 1).countTo()
    printInt(i)
}

fn printInt(i: int)
    printf("%d ", i)

extern_fn printf(char[], int)
