%%
#include <stdio.h>
%%

fn main()
    10.fibBelow()

fn fibBelow(i: int) {
    if (i > 0)
        (i - 1).fibBelow()
    i.fib().printInt()
}

fn fib(i: int): int =
    if (i == 0)
        0
    else if (i == 1)
        1
    else
        (i - 1).fib() + (i - 2).fib()

fn printInt(i: int)
    printf("%d ", i)

extern_fn printf(char[], int)
