import std.c.stdio

typealias string = char[]
typealias string_container = Container<string>

struct Container<T> {
    val value: T
}

fn main(): int {
    "Hello, World!".Container<string>().Container<string_container>().value.value.printf();
    return 0;
}
